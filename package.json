{
  "name": "van-cli",
  "version": "0.0.1-beta.1",
  "description": "TypeScript CLI wrapper for the NGP VAN API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "van": "./bin/cli.js",
    "van-cli": "./bin/cli.js",
    "ea": "./bin/cli.js"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "dev": "node dist/cli.js",
    "test": "npm run clean && npm run build && vitest run",
    "test:coverage": "npm run build && vitest run --coverage",
    "prepack": "npm run clean && tsc -p tsconfig.build.json"
  },
  "keywords": [
    "ngp",
    "van",
    "api",
    "cli",
    "political",
    "crm",
    "typescript"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  }
}
